# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository (`tstprw`) currently contains no source code, build configuration, tests, or tooling — only a `README.md` describing it as "Testes Git x TDS" (Portuguese: Git tests with TDS). It appears to be a scratch repository used for experimenting with Git workflows.

There is therefore no build, lint, test, or run command to document yet. When code is added, update this file with the relevant commands and an architecture overview.

## Branches

- `master` — default branch.
- Feature work happens on dedicated branches (e.g. `claude/claude-md-docs-YMKJk`). Do not push directly to `master` without explicit instruction.
